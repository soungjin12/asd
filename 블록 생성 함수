const blocks = [];

const box =
new THREE.BoxGeometry(
1,
1,
1
);

function makeBlock(
x,
y,
z,
color
){

const mesh =
new THREE.Mesh(

box,

new THREE.MeshLambertMaterial({
color
})

);

mesh.position.set(
x,
y,
z
);

scene.add(mesh);

blocks.push(mesh);

}
